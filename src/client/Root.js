import React from "react";
import App from "../Components/App";
import { HashRouter } from "react-router-dom";

const Root = () => {
  return (
    <HashRouter>
      <App />
    </HashRouter>
  );
};

export default Root;
